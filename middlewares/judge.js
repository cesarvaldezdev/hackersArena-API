const hackerEarth = require('hackerearth-node');

const hackerEarthConnection = new hackerEarth(
  process.ENV.CLIENT_SECRET_KEY,
  '0'
)

class Judge {
  constructor(config = {}) {
    this.config = config;
  }

//  static async compile(config, (err, res) => {});
}
var config = {};
config.time_limit = 1;
config.memory =

modules.exports = new  Judge();
modules.exports = hackerEarthConnection;
